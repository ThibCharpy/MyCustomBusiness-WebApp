import React from 'react';

/**
 * User component to display user's informations
 */
class UserItem extends React.Component {

    /**
     * Render html and other components function
     */
    render() {
        return (
            <tr>USER</tr>
        );
    }
}

export default UserItem;